<%= pluralize( @events.count, 'new event' ) %> for

<%= @alertable.short_name %> on <%= @alertable.chain.name %>
<%= namespaced_path(@alertable.chain.alertable_type, @alertable, chain: @alertable.chain, full: true) %>

There <%= @events.count == 1 ? 'is' : 'are' %> <%= pluralize( @events.count, 'new event' ) %>.

<%= render partial: 'event_list' %>
