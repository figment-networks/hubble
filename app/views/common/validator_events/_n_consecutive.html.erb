<div>
  <span class='d-inline d-md-none <%= icon_classes %> mr-1'></span>
  <span class='font-weight-bold'>
    <% if @validator.nil? %>
      <%= link_to event.validatorlike.short_name, namespaced_path( 'validator', event.validatorlike ) %> missed
    <% else -%>
      Missed
    <% end -%>
    <span class='technical'><%= event.data['n'] %></span> consecutive precommits
  </span>
  <span class='text-muted'>
    as of block
    <%= link_to event.height, namespaced_path( 'block', event.height ), class: 'technical' %>
  </span>
</div>
