<div class="modal fade" id="snapshotModal" tabindex="-1" role="dialog" aria-labelledby="snapshotModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="snapshotModalLabel">
          <i class="fas fa-camera mr-2"></i> Snapshot
        </h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        The snapshot for Cycle <%= @cycle.number %> was taken in Cycle
        <%= @cycle.snapshot_cycle_number %> at Block <%= number_with_delimiter @cycle.snapshot_height, delimiter: "," %>.
        The snapshot determines how many blocks each account is entitled to Bake and
        Endorse in this cycle. Rewards for this cycle will be split based on
        delegated balances as of the snapshot.
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

<div class="modal fade" id="rewardsModal" tabindex="-1" role="dialog" aria-labelledby="rewardsModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="rewardsModalLabel">
          <i class="fas fa-icicles mr-2"></i> Rewards Unfrozen
        </h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Rewards earned for Bakes and Endorsements in Cycle <%= @cycle.number %>
        will be unfrozen and available after Cycle <%= @cycle.rewards_unfrozen_cycle_number %>.
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>
